{
  "name": "@jb/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fastify API: engines orchestration, Claude gateway, workflow and audit",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/sensible": "^6.0.0",
    "@jb/contracts": "*",
    "@jb/db": "*",
    "drizzle-orm": "^0.45.0",
    "fastify": "^5.4.0",
    "fastify-plugin": "^5.0.0",
    "jose": "^6.2.12",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0",
    "tsx": "^4.20.0"
  }
}
